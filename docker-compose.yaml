services:
   postgres:
      image: postgres:16
      container_name: postgres_atividade_node
      environment:
         POSTGRES_USER: admin
         POSTGRES_PASSWORD: admin
         POSTGRES_DB: mydb
      ports:
         - "12345:5432"
      volumes:
         - pgdata:/var/lib/postgresql/data

volumes:
   pgdata:
